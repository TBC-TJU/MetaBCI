<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nj.back.dao.FileMapper">
    <insert id="updateFile" parameterType="com.nj.back.pojo.File">
        INSERT INTO file (name, type, content, upload, agree,tag,date)
        VALUES (#{name}, #{type},#{content}, #{upload}, #{agree},'NO',CURRENT_DATE)
    </insert>
</mapper>